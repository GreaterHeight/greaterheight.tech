# Build Notes

This migration intentionally uses a faithful-first approach. The captured DNN website remains the reference for content and visual behavior; DNN/WebForms runtime dependencies are removed from the rebuilt HTML. Modernization should be applied after visual parity is established.
