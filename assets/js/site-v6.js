(()=>{function ready(){const nav=document.querySelector('.gh-nav'),menu=document.querySelector('.gh-menu-toggle');if(nav&&menu){menu.addEventListener('click',()=>{const open=nav.classList.toggle('is-open');menu.setAttribute('aria-expanded',String(open));});nav.querySelectorAll('.gh-nav__toggle').forEach(btn=>btn.addEventListener('click',()=>{const item=btn.closest('.gh-nav__item');const open=item.classList.toggle('is-open');btn.setAttribute('aria-expanded',String(open));}));document.addEventListener('keydown',e=>{if(e.key==='Escape'){nav.classList.remove('is-open');menu.setAttribute('aria-expanded','false');nav.querySelectorAll('.gh-nav__item.is-open').forEach(x=>x.classList.remove('is-open'));}})}const top=document.querySelector('.gh-top');if(top){const update=()=>top.classList.toggle('is-visible',scrollY>500);addEventListener('scroll',update,{passive:true});update();top.addEventListener('click',e=>{e.preventDefault();scrollTo({top:0,behavior:'smooth'})})}document.querySelectorAll('[data-contact-form]').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const hp=form.querySelector('[name=website]');if(hp&&hp.value)return;const status=form.querySelector('[data-form-status]');if(status)status.textContent='Thanks. Your message has been captured in this static demonstration. Connect the form endpoint before launch.';}));document.querySelectorAll('a[href="#"]').forEach(a=>a.addEventListener('click',e=>e.preventDefault()));}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',ready):ready()})();